package labmus.zebrafish_utils.utils;

import org.bytedeco.opencv.opencv_core.Mat;

import java.util.function.Function;

public class ImageProjectFunction implements Function<Mat, Mat> {
    @Override
    public Mat apply(Mat mat) {
        return null;
    }
}
