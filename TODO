-> change apply brightness function (in ZF Helper Methods) to use openCV one

-> crop heatmaps output
    -> Mat cropped = new Mat(masked, roi);

-> FFmpegPlugin fps change not working properly. It seems only increasing it is working.
-> FFmpegPlugin needs a scale setting to reduce the video resolution.