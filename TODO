-> FFmpeg plugin repeats the first frame of videos converted to AVI (probably to all of them), doing it from blank->end
-> change apply brightness function to use openCV one
-> heatmap-images needs a proper processing sequence written
-> use auto-generated colors in sectors score (like in frequency data)

-> Write documentation
    -> Suggest the usage of that excel export plugin  https://imagej.net/plugins/excel-functions
    -> provide more test samples, ready to be tested ones, to be linked in each tool/feature
        -> https://drive.google.com/drive/folders/1qrz53eYvl06PCNzTaGLvaBCJtKP0Ubd8?usp=drive_link
    -> quantify heatmap input image

-> Extract Frequency Data
    -> bouts/peaks plot?
-> Extract Tracking Data?
-> Thigmotaxis
    -> internal area is 50% of external area

-> crop heatmaps output
    -> Mat cropped = new Mat(masked, roi);
-> ROI creator scale always in pixel
