-> change apply brightness function (in ZF Helper Methods) to use openCV one

-> crop heatmaps output
    -> Mat cropped = new Mat(masked, roi);

-> add documentation for ffmpeg invert colors
-> progress bar in ffmpeg should consider the select filter when skipping frames (probably -vframes option will be useful)