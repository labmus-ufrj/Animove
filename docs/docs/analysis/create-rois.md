# Create Sequential ROIs

It creates circular ROI sections according to the number and size specified by the user. The size unit will depend on the scale previously stated. The plugin creats the given number of ROI and open the ROI manager.

