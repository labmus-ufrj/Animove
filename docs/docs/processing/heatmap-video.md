# Generate Heatmap Video

<!--
info:
essa macro faz a mesma coisa que o heatmap images só que uma vez por frame do vídeo original.
-->

## Don't save, open in ImageJ instead
The disk space is still needed for processing, this just marks all temp files for exclusion when the JVM closes. A virtual stack will be imported from the loose frames.